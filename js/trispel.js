

// Only replace the menu on mobile phones.
/*
      if (window.innerWidth < 600) {
        var header = $('.header-bottom');
        header.hide();
        // var menuButton = $('<div class="mobile-nav-wrapper"><a class="btn-mobile-nav" href="#">☰</a></div>');
        var menuButton = $('<div class="mobile-nav-wrapper"><a class="btn-mobile-nav" href="#"></a></div>');
        menuButton.click(function(e) {
          e.preventDefault();
          header.toggle();
        });
        $('#logo').once('mobile-menu').after(menuButton);
      }
    }
  }; */
